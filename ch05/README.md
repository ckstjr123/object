## 응집도 낮은 클래스

---
- 클래스가 하나 이상의 변경 이유를 가짐<br>
  → 변경의 이유를 기준으로 클래스 분리
- 인스턴스 변수가 서로 다른 시점에 초기화되거나 일부만 초기화 됨<br>
  → 함께 초기화되는 속성을 기준으로 클래스 분리
- 메서드들이 사용하는 속성에 따라 그룹이 나뉨<br>
  → 속성 그룹과 해당 그룹에 접근하는 메서드 그룹을 기준으로 클래스 분리